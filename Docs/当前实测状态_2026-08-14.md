# 当前实测状态（2026-08-14 18:30）

## 已在游戏内确认

- 原有冲量锤、碰撞连锁、敌人攻击、血条、绳线枪、调质、低摩擦、撞墙爆发与落地震荡均无明显问题。
- 最小狩猎闭环基本通过：器官掉落与拾取、`ORGANS`、10 怪合同计数、清场开门、撤离结算和狩猎摘要均可进入实际测试流程。
- 遭遇账本输出 `Encounter started: state=ACTIVE total=10`，说明关卡中的 10 只敌人登记正确。
- `L_Prototype04.umap` 是当前新保存的灰盒关卡检查点，本次纳入 Git；默认启动地图暂不改动。

## 已修复、待复验

- Unity Build 中两个匿名命名空间辅助函数重名已经修复。
- `ExtractionLedger` 与 `OrganLedger` 故意测试非法输入时产生的 Warning 已降为 Verbose；重新编译后应不再显示 Automation 感叹号。
- 出口门开局 `remaining=0` 已修正：门先监听遭遇进度，只有账本进入有效合同且 `total>0` 后才打印锁定数字；待 PIE 确认输出 `remaining=10 total=10`。

## 当前唯一明确异常（已实施修复，待复验）

- 升空叉已实测，但玩家视觉反馈为“好像没效果”。
- 已针对目标未命中和移动模式时序实施保守修复：射程 360→650cm、优先鼠标射线最对齐目标、命中黄色脉冲、失败红色搜索线、速度成功排队后才切 Falling。
- 下一轮只需捕获一次操作后的两类日志：

```text
LogVectorLiftFork: ...
LogVectorMovement: Velocity override consumed: ...
```

- 正确链路应为：按 `5` 只选择 `LIFT` → 靠近轻型目标至 650cm 内 → 鼠标指向目标 → 点左键 → 黄色脉冲 → `Lift fork hit ... queued=OK ... mode=Falling` → 下一帧 `target=V(Z=...)`。

## 新批准的后续范围

- 用户已批准正式 PCG 与 Boss 进入后续开发，但要求先设计且不得抛弃物理玩法。
- 设计基线见 `Design/正式PCG与物理Boss设计_v0.2.md`；实现顺序为确定性战术模块模型 → 灰盒生成器 → 物理 Boss 状态机 → 战斗整合。
- 营地制造、永久升级、过载节点和正式美术仍暂缓，避免与 PCG/Boss 同时扩大范围。

## 持续开发交付（等待用户侧编译/PIE）

- 已完成确定性 PCG 模块序列、战术硬约束/评分/回退与 4 项 Automation。
- 已完成 Editor Python 正式路线：SafeStart → 两个 Seed 决定的战术房 → BossRing → Extraction。
- 运行时按玩家入房依次激活 8 怪、8 怪、Boss+2；两道波次门、动态合同封账、撤离门与房间重生检查点已串联。
- 首个 `AVectorPhysicsBoss` 已实现锚定/暴露/过载三阶段、有限质量 8→4→3、锁向冲锋、物理落地震荡、过载低摩擦场地和 Boss HUD。
- 当前 Automation 注册口径为 40 项；完整统一验收见 `Docs/本轮持续开发统一验收_2026-08-14.md`。
- 上述新功能尚未经过用户侧 UHT/完整编译/PIE，状态为“已实现、待引擎验收”。
