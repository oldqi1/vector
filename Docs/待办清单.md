# 冲量荒原：当前状态与待办

> 更新：2026-08-17
> 用途：只记录当前证据、当前 30 小时任务和明确后置项，不保留已过期的阶段流水账。
> 产品方向见 [`冲量荒原_Demo系统设计_v0.6.md`](冲量荒原_Demo系统设计_v0.6.md)，实施顺序见 [`Demo30小时逐小时工作表_2026-08-17.md`](Demo30小时逐小时工作表_2026-08-17.md)。

## 1. 玩家已确认或已有 PIE 证据

- 基础冲量、质量差、碰撞守恒与反弹结果可用；
- 玩家/敌人血条、敌人伤害、死亡重生与虚空回收可用；
- 双端绳线、润滑/浮化、撞墙爆发、落地震荡基本可用；
- 升空叉能够产生实际升空；
- 器官拾取、遭遇账本、清场开门与撤离闭环能够运行；
- 矢量枪按有效质量换算速度，锚定结构有生命伤害衰减；
- 动能球可以被重新定向并参与结构碰撞；
- 清场三选一与规则模块已有运行日志；
- H01 完整 `VectorEditor / Development_Editor / x64` 编译通过，Editor Automation 为 **46 Success / 0 Failed**；
- H02 Development 基线包已生成并完成关闭编辑器后的冷启动；默认地图、玩家控制和怪物模型均正常，软引用怪物资产已通过 Always Cook 保持编辑器/包体一致。
- H03 垂直换向纯数学已完成：固定 `k=0.6、η=0.88、Floor=700`，静止/800/1400/2400 与 NaN 防护通过离线镜像、完整编译和 **47 Success / 0 Failed** Editor Automation；
- H04 升空叉运行时高低速分支已有 PIE 证据：低速 `preSpeed=150/179/212` 严格输出 `(0,0,700)`，Boss 冲锋 `preSpeed=1436` 输出 `postVh=505/postVz=1158`，均为 `check=PASS`；
- H05 地面/空中分流已由玩家确认无问题：同一离地周期不再重复获得保底；后续平衡修订将基础保底从 700 降为 520。
- H06–H08 目标选择、空中影子、真实首次阻挡轨迹、静态误差和动态首碰预览已由玩家确认无问题；有效静态样本均为 PASS。
- Boss 推入虚空后能够按环境处决完成遭遇结算，不再保留不可达目标。

这些证据不自动覆盖后续未编译改动；每轮新增反射类或数学修改后仍需重新编译和 Automation。

## 2. 已实现，等待当前编译和/或 PIE 玩法验收

- [~] H10 升空链闭环：基础升空叉以 `520cm/s` 抬升且不造成稳定伤害，自然落下使用 `450` 门槛、`0.5×` 半径、`0.35×` 伤害、8 点上限的低震荡；`5→1` 消耗升空标记，让矢量枪获得 `1.25×` 注入冲量并恢复完整落地档案。`LIFT-VECTOR COUPLER` 解锁长按自动砸向 500cm 内最密集怪群，拖动则手动选点。等待完整编译、当前源码 51 项 Automation 与 PIE；
- [~] 电弧壳机：0.7 秒预警、有限弱追踪蓝球；
- [~] 腐蚀无人机：单发/三向绿色实体弹交替；
- [~] 矢量枪命中蓝球后解除导引并保留其物理速度；
- [~] 两种远程怪进入普通 PCG 房功能槽；
- [~] 远程怪受外力/失衡时取消蓄力攻击；
- [~] 弹丸不进入 `AVectorEnemy` 清房账本；
- [~] Boss Resolve 抗无限僵直、空中反击与循环动能弹补给；
- [~] PCG 接缝门不影响 NavMesh、敌人 Nav 投影/恢复和房间出口校验。
- [~] H11 通用环境换向器：仅消费 `ImpulseDriven` 目标，把完整入射速度以 `0.88×` 预算旋转到装置出口方向；同一次重叠只触发一次，完全离开后复位。已完成纯数学、运行时 Actor、51 项 Automation 注册与离线镜像，等待完整编译/PIE；
- [~] H12 HeightShelf 双路线灰盒脚本：路线 A 为“下层冲锋源→环境换向器→上层接收甲板”，路线 B 为“矢量注入→升空叉→定向砸入低层怪群”；两条路线拥有不同首操作，等待重跑脚本、Build Paths 与 PIE。
- [~] H13 HeightShelf 删除 Gate：PCG Recipe 已能精确计算删除某节点后的剩余路线；移除环境换向器或禁用升空叉均为 `2→1`，两者都移除为 `2→0`。装置不进入敌人账本，删除模式仍保留基础清场路径；离线镜像通过，等待 PIE 验证实际高度价值与开门。
- [~] H15 Demo 路线硬约束：Seed 继续决定第一普通房，但第二遭遇现在必须且只出现一次 `HeightShelf`，随后才是 Boss；错误顺序会被 PCG Validate 拒绝，避免合法 Seed 绕过核心高度教学。等待编译/Automation 与重生成地图确认。
- [~] H18 成长顺序修正：第一房清场直接出现规则模块三选一，选择一次即可改变下一间 HeightShelf；后续清场才进入射程/冲量/容量/充能校准。`LIFT-VECTOR COUPLER` 不再晚于它负责的高度考试出现，等待 PIE 验证选择提示和下一房触发反馈。
- [~] H17 Boss 追猎合同：攻击循环覆盖半径由 1800 扩到 3000cm；锚定/破一组锚/过载的追击速度依次为 210/285/360cm/s，同时有效质量依次下降。破结构后既更可移动也更主动追人，等待 PIE 调节压迫感。
- [~] H16 远程打断合同：预警被外部冲量/僵直打断后进入 0.75s 重试冷却，既奖励打断也不会落地立刻重开预警；等待 PIE 验收。
- [x] H18 路线配置硬门：运行时 Director 明确拒绝第二战斗房非 HeightShelf 的旧地图数据。
- [~] H19/H20 战术电路可读性：新增不参与碰撞、Nav 和账本的常驻角色标记；HeightShelf 已把真实动能源/换向器/接收区标成橙/青/紫红，等待重生成地图后验收俯视镜头可读性。
- [~] H20 敌人角色轮廓：导入模型不支持变色时，冲锋怪仍有前向角锥+橙光，电弧壳有顶部蓝球+蓝光，腐蚀无人机有顶部绿盘+绿光，重型保留可拆双锚+紫光；指示几何均不参与碰撞与 Nav。
- [~] H22 最小入口：HUD 常驻显示与场内标记同色的 `SOURCE > CONVERTER > RECEIVER`，SafeStart 提示先读机器再向东推进，战斗中改为“蓄速—改路—兑现”；等待分辨率与遮挡验收。
- [~] H20 Boss 阶段可读性：破锚除了移除实体锚脚外会短促亮起；两组破坏进入 Overload 才显示独立红色核心球，核心几何不参与碰撞、Nav 或伤害判定。
- [~] H16 齐射可读性回归：蓝球固定单弹，绿弹严格按 `1→3→1` 循环；预警线数与实际生成数改为共用同一纯函数，新增第 51 项 Automation 注册固定数量与 `-14/0/+14°` 对称性。
- [~] H22 暂停/退出：`Esc` 能在游戏暂停时继续触发并切换暂停，遮罩明确显示 `Esc Resume / Q Quit`；`Q` 只在暂停状态接受，防止战斗中误退。
- [~] H18 成长选择可读性：规则模块从一行纯名称改为三行“键位 + 规则变化”，选择前可直接读到射击高速目标返还弹药、第二目标传递 45% 或空中导向+自动/手动砸落。
- [~] H21 三档碰撞反馈：所有可作为弹药的目标在共享碰撞事实上触发独立短脉冲，`300/700/1200cm/s` 以上分别为青/黄/紫红；持续时间只有 `0.09/0.13/0.18s`，不覆盖攻击预警、升空和角色灯。
- [~] H19 灰盒基础光层：PCG 重生成脚本统一创建中性方向主光和每模块独立冷色填光，距离衰减不跨房污染橙/青/紫红战术节点语义；脚本重跑时会连同旧 `PCG_` 灯一起替换，不叠加。
- [~] H19 语义几何着色：战术节点地面标记、冲锋/远程怪角色几何和 Boss 暴露核心都使用独立动态材质颜色，不再只靠点光染色；与输入模型自带材质分离。
- [~] H18 成长触发反馈：选择后 HUD 显示 2.5s `MODULE INSTALLED`；下一房触发退弹、双目标传递、破锚或砸落时显示 1.6s `MODULE TRIGGERED`，反馈由实际成功分支发出而不是按键即发。
- [~] H22 工具句法入口：SafeStart 非模态卡片直接把 `1/2/3-4/5` 翻译为 `INJECT / GUIDE / CHANGE RULES / CHANGE PLANE`，进入首房后自动消失，不要求玩家先读外部文档。

## 3. 当前唯一执行队列：30 小时可分发验证包

完整逐小时任务不在本文件复制，避免两个排期漂移。阶段状态如下：

- [x] H01–H02：完整编译、Automation 与首个可冷启动 Development 包；
- [~] H03–H13：H03–H08 已完成并获 PIE 确认；H09–H10 已实现、待最新编译/Automation/PIE；H11 环境换向器与 H12 HeightShelf 双路线已实现首版并通过离线验证，H13 删除测试与软锁保护正在推进；
- [ ] H14–H18：回归 PCG/远程敌群/Boss/成长并闭合整局路线；
- [ ] H19–H22：统一灰盒视觉、战斗反馈、最小音频和新玩家入口；
- [ ] H23–H27：候选包、完整跑通、单轮调参、阻塞修复和 RC；
- [ ] H28–H30：RC 连续回归、最终压缩包、说明文档和实际交付。

强制 Gate：

1. H02 首个可启动包；
2. H05 换向数学；
3. H08 首次阻挡预测误差；
4. H13 HeightShelf 双路线和删除测试；
5. H18 第一房到撤离完整路线；
6. H27 Release Candidate；
7. H30 实际包体交付。

任一 Gate 失败，后续工时转为修复，不继续扩内容。

## 4. 30 小时后候选，当前不做

- Boss 顶部井口结构与垂直入射判定；
- 高处绳线锚点、玩家主动上层与真正三维墙面牵引；
- 磁壳搬山机持续追猎、远程封位与破壳后换题；
- 腐蚀持续伤害/地面污染；
- 精英房独立敌人；
- 第二/第三 Boss；
- 中继节点正式 UI、商店/宝箱房、完整跨局制造；
- 正式机械模型、骨骼动画、完整 VFX/音频和 Shipping 包装。

只有 HeightShelf Go 后，Boss 井口与高处绳线才进入下一轮。

## 5. 当前技术注意项

- 新增 UCLASS/UENUM/UPROPERTY 后，关闭 Editor 或关闭 Live Coding 再完整编译；
- 本机命令行无头 Automation 会卡 ZenServer，使用 Session Frontend；
- 重跑 PCG 灰盒脚本后执行 `Build Paths`；
- 绳线使用自定义三维单向张力约束，不是 Chaos 绳段；
- 弹丸必须继续与遭遇账本隔离；
- DrawDebug 只用于验证，打包前关闭高频调试绘制；
- 任何“日志 PASS 但玩家看不懂”的结果都不算玩法验收通过。
